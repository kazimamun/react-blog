import React from 'react';
import './Card.css'
import Logo from '../../Logo/Logo';

const Card = (props) => {
    return (
        <div className="card">
            <Logo></Logo>
        </div>
    );
};

export default Card;