import React from 'react';

const Header = (props) => {
    return (
        <header>
            <nam>
                <a href="#home">Home</a>
                <a href="#about">About Us</a>
                <a href="#contact">Contact us</a>
            </nam>
        </header>
    );
};

export default Header;