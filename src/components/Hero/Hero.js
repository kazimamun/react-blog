import React from 'react';
import './Hero.css';
import Card from '../UI/Card/Card';

const Hero = (props) => {
    return (
        <div>
            <Card />
        </div>
    );
};

export default Hero;