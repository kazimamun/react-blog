import React from 'react';
import './style.css';
import Header from '../../components/Header';

const Home = (props) => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Home;